const state = {
    loggedInUser: {

    },
    isLoggedIn: false
}

const getters = {
    loggedInUser: state => state.loggedInUser,
    isLoggedIn: state => state.isLoggedIn
}

const actions = {
}

const mutations = {

}

export default {
    state,
    getters,
    actions,
    mutations
}
